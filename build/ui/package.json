{
  "private": true,
  "name": "@app/ui",
  "scripts": {
    "clean": "rm -rf ../../built/ui",
    "compile": "npm run clean && tsc -p tsconfig.inject.json",
    "injectify": "bash -x scripts/convert_to_client_side_injectable.sh",
    "build:artifacts": "npm run compile && npm run injectify && bash ../../code/ui/test/raw/test_settings_dialog.sh",
    "pretest": "npm run build:artifacts",
    "test": "jest --config=jest.config.js",
    "build": "npm run build && npm run test"
  }
}


