{
  "private": true,
  "name": "@app/ui",
  "documentation": {
    "prebuild:ui": "Prebuild step for the UI package. This navigates to the `common` package and runs its `dist` target so common code used by ui is available",
    "clean": "Cleans the UI build output directory by removing the `dist/ui` folder.",
    "build:ui": "runs webpack to transpile TypeScript code to format suitable for inclusion in *Dialog.html and for deployment.",
    "setup:fixture": "Sets up a test fixture for the `SettingsDialog` UI component. This builds the UI package and creates a monolithic HTML file for ad hoc testing in a browser.",
    "build": "Builds the UI package, sets up the test fixture, and runs Jest tests using the specified configuration file.",
    "predist": "Prepares the GAS-safe staging directory by creating the necessary folder structure.",
    "dist": "Copies built UI raw assets to the GAS-safe staging directory."
  },
  "scripts": {
    "prebuild:ui": "cd ../common && npm run dist",
    "clean": "rm -rf ../../dist/ui",
    "build:ui": "npm run clean && node ../../build/ui/build_all_ui_logic.js",
    "setup:fixture": "npm run build:ui && bash ../../code/ui/test/raw/create_dialog_test_fixture.sh SettingsDialog",
    "build": "npm run setup:fixture && if [ \"$SKIP_TESTS\" != \"true\" ]; then jest --config=jest.config.js; fi",
    "predist": "mkdir -p ../../dist/ui/gas_safe_staging",
    "dist": "npm run build && cp -a ../../code/ui/src/raw/*.html ../../dist/ui/gas_safe_staging/"
  },
  "devDependencies": {
    "@testing-library/dom": "^9.3.3",
    "@types/jest": "^29.5.14",
    "@types/node": "^18.19.126",
    "jest": "^29.7.0",
    "jsdom": "^24.0.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@types/testing-library__dom": "^7.0.2"
  }
}