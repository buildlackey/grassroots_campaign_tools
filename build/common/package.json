{
  "private": true,
  "name": "@app/common",
  "scripts": {
    "clean": "rm -rf ../../dist/common/gas_safe_staging",
    "test": "jest",
    "build": "npm run clean && tsc --project tsconfig.json",
    "dist": "npm run build && if [ \"$SKIP_TESTS\" != \"true\" ]; then npm run test; fi"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "@types/jest": "^29.5.14",
    "typescript": "^5.4.0"
  }
}
