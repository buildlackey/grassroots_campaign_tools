{
  "private": true,
  "name": "@app/common",
  "scripts": {
    "clean": "rm -rf ../../dist/common/gas_safe_staging",
    "dist": "npm run clean &&  tsc --project tsconfig.json",
    "build:standalone-logger": "npm run clean &&  tsc --project tsconfig.standalone.json"
  }
}
