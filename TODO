
when we save settings.. for sheet1 default comes up as 'Address'
if we override and choose other col (e.g., 'Name').. it does not 'stick'
when we bring up dialog again it should show Name.. but goes back to 'Address'

fix undo in editor (idea).    'U' not working

look into issue of chrome window not on top when we click


settings dialog - not responding to dismiss / (save?)   esc


unit_testable -> better name? needed?

run a test where we deliberately save with an error (e.g., maps key is empty)
  also.. how about :  select a sheet tab w/ no col's and then call extra button..
    regular save button should be disabled


change built -> dist
change unit_testable to something more reflective of purpose


webpack.gas.config.js
    change devtool: -> false   (for production).
